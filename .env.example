# API Keys
GROQ_API_KEY=your-groq-api-key-here
TAVILY_API_KEY=your-tavily-api-key-here

# Confluence MCP Server Configuration (for Docker Compose)
CONFLUENCE_ENABLED=true
CONFLUENCE_URL=https://your-domain.atlassian.net
CONFLUENCE_USERNAME=your-email@example.com
CONFLUENCE_API_TOKEN=your_confluence_api_token_here

# Note: When running locally (non-Docker), CONFLUENCE_MCP_URL should be http://localhost:3001
# When running in Docker, the service name (mcp-server) is used for internal networking
